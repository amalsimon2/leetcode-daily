# Explanation

Since reducing an element repeatedly by the maximum multiple of the threshold until it is less than the threshold is mathematically equivalent to finding the remainder when divided by the threshold (modulo operation), we can simply apply the modulo operator to each element in the array.

## Time Complexity

O(N)

## Space Complexity

O(N)
